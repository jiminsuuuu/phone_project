extends ../layouts/base.pug

block content 
        div.signupBox
            form(action="/informations" method="POST")
                div.NAME
                    label(htmlFor="name") NAME : 
                    input(type="name" name="name" required)
                div.BIRTH
                    label(htmlFor="birth") BIRTH : 
                    input(type="text" name="birth" required)
                div.GENDER
                    label(htmlFor="gender") GENDER :  
                        select(name="gender" required)
                            option(value="male") 남성
                            option(value="female") 여성
                div.PHONENUM
                    label(htmlFor="phonenum") PHONE NUMBER : 
                    input(type="text" name="phonenum" required)
                div.EMAIL
                    label(htmlFor="email") E-MAIL : 
                    input(type="email" name="email" required)
                div.PASSWORD
                    label(htmlFor="password") PASSWORD :  
                    input(type="password" name="password"  required)   
                input(type="submit" name="회원가입하기")

